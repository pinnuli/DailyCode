
package the51_数组中的逆序对;

import org.junit.Test;
import org.junit.Before;
import org.junit.After;

/**
 * Solution1 Tester.
 *
 * @author <Authors name>
 * @version 1.0
 * @since <pre>三月 22, 2019</pre>
 */
public class SolutionTest {

    @Before
    public void before() throws Exception {
    }

    @After
    public void after() throws Exception {
    }

    /**
     * Method: InversePairs(int [] array)
     */
    @Test
    public void testInversePairs() throws Exception {
        Solution solution = new Solution();
        System.out.println(solution.InversePairs(new int[]{7, 5, 6, 4}));
    }


    /**
     * Method: mergeSort(int[] array, int low, int high)
     */
    @Test
    public void testMergeSort() throws Exception {
//TODO: Test goes here... 
/* 
try { 
   Method method = Solution1.getClass().getMethod("mergeSort", int[].class, int.class, int.class);
   method.setAccessible(true); 
   method.invoke(<Object>, <Parameters>); 
} catch(NoSuchMethodException e) { 
} catch(IllegalAccessException e) { 
} catch(InvocationTargetException e) { 
} 
*/
    }

    /**
     * Method: merge(int[] array, int low, int mid, int high)
     */
    @Test
    public void testMerge() throws Exception {
//TODO: Test goes here... 
/* 
try { 
   Method method = Solution1.getClass().getMethod("merge", int[].class, int.class, int.class, int.class);
   method.setAccessible(true); 
   method.invoke(<Object>, <Parameters>); 
} catch(NoSuchMethodException e) { 
} catch(IllegalAccessException e) { 
} catch(InvocationTargetException e) { 
} 
*/
    }

} 
